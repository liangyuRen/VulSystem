server.port=${SERVER_PORT:8081}

# Database Configuration - Using environment variables for Docker deployment
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:15256785749rly}
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:kulin}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.sql.init.mode=never

spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# File upload directory - supports both local and Docker paths
#file.upload-dir=${FILE_UPLOAD_DIR:/app/uploads/}
file.upload-dir=D:\\kuling\\upload

# OpenSCA tool path - supports both local and Docker paths
#opensca.tool-path=${OPENSCA_TOOL_PATH:/app/opensca/}
opensca.tool-path=D:\\kuling\\opensca